"""Turning retrieved passages into an answer, and proving the answer stands on them.

`prompt.py` asks; `citations.py` imposes. Between them sit the decision of whether a turn
needs retrieval at all, the conversation it belongs to, and the transport that streams it.
"""
